extends layout

block content
        .row
            .col-md-8.offset-md-2.about-content-main.col-xs-12
                h1.display-4.m-b-2.text-xs-center #{title}
                p {{ bacon }}
                a.btn.btn-info.btn-block(href='/register') Free Registration!
